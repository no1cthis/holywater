import { SectionEnum, type SectionEnumType } from "./types";

// Default bucket name used in the application
export const BUCKET_NAME = 'holywater-bucket';

// Array of section types that should save movie items
export const SECTIONS_WITH_MOVIES: SectionEnumType[] = [SectionEnum.HeroSlider];
